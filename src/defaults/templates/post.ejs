<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= metadata.title %> | Pagebrew</title>
    <meta name="description" content="<%= metadata.description %>">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body class="bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100">
    <%- include('partials/header.ejs') %>
    
    <main class="mx-auto px-4 py-8 max-w-screen-md">
        <article class="prose dark:prose-invert max-w-none lg:prose-lg mx-auto">
            <header class="mb-8">
                <h1 class="mb-2"><%= metadata.title %></h1>
                <div class="text-sm text-gray-600 dark:text-gray-400">
                    <%= metadata.date.toLocaleDateString() %>
                    <% if (metadata.tags && metadata.tags.length > 0) { %>
                        • 
                        <% metadata.tags.forEach((tag, index) => { %>
                            <span class="inline-block bg-gray-100 dark:bg-gray-800 rounded px-2 py-1 text-sm">
                                <%= tag %>
                            </span>
                        <% }) %>
                    <% } %>
                </div>
            </header>
            <%- content %>
        </article>
    </main>

    <%- include('partials/footer.ejs') %>
</body>
</html> 