<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page.title %> | <%= siteMetadata.name %></title>
    <meta name="description" content="<%= page.description %>">
    <link href="/css/style.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="min-h-screen bg-rose-base font-space text-rose-text">
    <%- include('partials/header.ejs') %>

    <main class="max-w-2xl mx-auto px-6 py-12">
        <% if (posts && posts.length > 0) { %>
            <%
            const titleColors = ['text-rose-love', 'text-rose-foam', 'text-rose-gold'];

            posts.forEach((post, index) => {
                const colorClass = titleColors[index % titleColors.length];
            %>
                <article class="mb-16">
                    <h2 class="text-2xl <%= colorClass %> mb-2">
                        <a href="<%= post.url %>"><%= post.title %></a>
                    </h2>
                    <p class="text-rose-subtle text-sm mb-4">
                        <%= post.date.toLocaleDateString('en-US', {
                            year: 'numeric',
                            month: 'long',
                            day: 'numeric'
                        }) %>
                    </p>
                    <p class="text-rose-text/90 leading-relaxed">
                        <%= post.description %>
                    </p>
                </article>
            <% }); %>
        <% } %>
    </main>

    <%- include('partials/footer.ejs') %>
</body>
</html>
